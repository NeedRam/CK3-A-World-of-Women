awow_perform_culling = {
	# Reset the running total before we count this pass.
	set_global_variable = {
		name = awow_cull_count
		value = 0
	}

	# Single pass: every living male that can be culled is killed and counted.
	# (Doing the flag round-trip in two separate loops was unreliable here.)
	every_living_character = {
		limit = {
			can_be_culled = yes
		}
		death = natural
		change_global_variable = {
			name = awow_cull_count
			add = 1
		}
	}

	# Reset the cycle counter so the next cull is scheduled from now.
	set_global_variable = {
		name = awow_months_since_cull
		value = 0
	}

	# Debug trace - check Documents/Paradox Interactive/Crusader Kings III/logs for this line.
	debug_log = "AWOW: cull complete, males taken = [global_var:awow_cull_count]"
}
